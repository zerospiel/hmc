---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
    helm.sh/resource-policy: keep
  name: clusteripams.k0rdent.mirantis.com
spec:
  group: k0rdent.mirantis.com
  names:
    kind: ClusterIPAM
    listKind: ClusterIPAMList
    plural: clusteripams
    singular: clusteripam
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - description: Phase
          jsonPath: .status.phase
          name: phase
          type: string
        - description: Time elapsed since object creation
          jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1beta1
      schema:
        openAPIV3Schema:
          description: ClusterIPAM is the Schema for the clusteripams API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: spec defines the desired state
              minProperties: 0
              properties:
                clusterIPAMClaimRef:
                  description: clusterIPAMClaimRef is a reference to the [ClusterIPAMClaim] that this [ClusterIPAM] is bound to.
                  minLength: 1
                  type: string
                  x-kubernetes-validations:
                    - message: Claim reference is immutable once set
                      rule: oldSelf == '' || self == oldSelf
                provider:
                  description: provider identifies the provider that will consume this claim
                  enum:
                    - in-cluster
                    - ipam-infoblox
                  type: string
              type: object
            status:
              description: status describes the observed state
              minProperties: 1
              properties:
                phase:
                  description: phase is the current phase of the ClusterIPAM.
                  enum:
                    - Pending
                    - Bound
                  example: Pending
                  type: string
                providerData:
                  description: |-
                    providerData is the provider specific data produced for the ClusterIPAM.
                    This field is represented as a list, because it will store multiple entries
                    for different networks - nodes, cluster (pods, services), external - for
                    the same provider.
                  items:
                    description: ClusterIPAMProviderData is the data of IPAM Provider.
                    minProperties: 1
                    properties:
                      config:
                        description: config contains the IPAM provider-specific data
                        x-kubernetes-preserve-unknown-fields: true
                      name:
                        description: name of the IPAM provider data
                        minLength: 1
                        type: string
                      ready:
                        description: ready indicates that the IPAM provider data is ready
                        type: boolean
                    type: object
                  minItems: 0
                  type: array
                  x-kubernetes-list-type: atomic
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
