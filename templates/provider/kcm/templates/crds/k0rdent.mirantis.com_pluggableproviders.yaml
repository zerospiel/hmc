---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: pluggableproviders.k0rdent.mirantis.com
spec:
  group: k0rdent.mirantis.com
  names:
    kind: PluggableProvider
    listKind: PluggableProviderList
    plural: pluggableproviders
    shortNames:
    - pprov
    singular: pluggableprovider
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.exposedProviders
      name: Providers
      type: string
    - jsonPath: .spec.description
      name: Description
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PluggableProvider is the Schema for the PluggableProvider API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PluggableProviderSpec defines the desired state of PluggableProvider
            properties:
              clusterGVKs:
                description: ClusterGVKs defines the Group-Version-Kind resources
                  this provider can manage
                items:
                  description: |-
                    GroupVersionKind unambiguously identifies a kind. It doesn't anonymously include GroupVersion
                    to avoid automatic coercion. It doesn't use a GroupVersion to avoid custom marshalling
                    Note: mirror of https://github.com/kubernetes/apimachinery/blob/v0.32.3/pkg/runtime/schema/group_version.go#L140-L146
                  properties:
                    group:
                      type: string
                    kind:
                      type: string
                    version:
                      type: string
                  required:
                  - group
                  - kind
                  - version
                  type: object
                type: array
              clusterIdentityKinds:
                description: ClusterIdentityKinds defines the Kind of identity objects
                  supported by this provider
                items:
                  type: string
                type: array
              config:
                description: |-
                  Config allows to provide parameters for management component customization.
                  If no Config provided, the field will be populated with the default
                  values for the template.
                x-kubernetes-preserve-unknown-fields: true
              description:
                description: Description provides a human-readable explanation of
                  what this provider does
                type: string
              template:
                description: |-
                  Template is the name of the Template associated with this component.
                  If not specified, will be taken from the Release object.
                type: string
            type: object
          status:
            description: PluggableProviderStatus defines the observed state of PluggableProvider
            properties:
              exposedProviders:
                description: ExposedProviders contains the list of exposed provider
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
