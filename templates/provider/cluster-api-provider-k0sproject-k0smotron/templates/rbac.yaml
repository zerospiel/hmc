apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: provider-interface-k0smotron
  labels:
    k0rdent.mirantis.com/aggregate-to-manager: "true"
rules:
  - apiGroups:
      - infrastructure.cluster.x-k8s.io
    resources:
      - remoteclusters
    verbs:
      - get
      - list
      - patch
      - watch
